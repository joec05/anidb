enum PaginationStatus{
  loading, error, loaded
}

enum LoadingState{
  loaded, loading, paginating, refreshing
}

enum AnimeBasicDisplayType{
  season, mostPopular, top, favourites, airing, upcoming
}

enum MangaBasicDisplayType{
  mostPopular, top, favourites
}

enum CharacterBasicDisplayType{
  top
}

enum AnimeRowDisplayType{
  myUserList, search
}

enum MangaRowDisplayType{
  myUserList, search
}

enum SelectedTitle{
  main, english, japanese
}

enum SelectedCharacterName{
  main, kanji
}
import 'package:anidb_app/global_files.dart';
import 'package:flutter/material.dart';

double defaultTextFontSize = 15.5;

double defaultHorizontalPadding = getScreenWidth() * 0.03;

double defaultVerticalPadding = getScreenHeight() * 0.01;

Size basicDisplayWidgetSize = Size(
  getScreenWidth() * 0.325,
  getScreenHeight() * 0.3,
);

Size basicDisplayCoverSize = Size(
  getScreenWidth() * 0.325,
  getScreenHeight() * 0.225,
);

Size completeDisplayCoverSize = Size(
  getScreenWidth() * 0.35,
  getScreenHeight() * 0.25,
);

Size detailDisplayCoverSize = Size(
  getScreenWidth() * 0.475,
  getScreenHeight() * 0.4,
);